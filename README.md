Veggie Finder locates vegetarian restaurants nearby using the Google Places API!

To deploy locally, add a .env file in the client and server directories with your Google Places API Key. Run 'npm start' in the client directory and 'node index.js' in the server directory. 
