Veggie Finder locates vegetarian restaurants nearby using the Google Places API!

To deploy locally, add a .env file in both the client and server directories with the contents: 'API_KEY={your_google_places_API_key}'. Run 'npm start' in the client directory and 'node index.js' in the server directory. You can then access the application at http://localhost:3000!
